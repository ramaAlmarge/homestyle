import 'package:flutter/material.dart';

class PaymantMethod extends StatelessWidget {
  const PaymantMethod({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
appBar: AppBar(),

    );
  }
}
